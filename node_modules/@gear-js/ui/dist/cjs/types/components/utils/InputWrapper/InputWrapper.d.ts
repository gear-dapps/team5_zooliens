import { ReactNode } from 'react';
import { Gap } from '../../../types';
import styles from './InputWrapper.module.scss';
declare type Props = {
    id: string;
    direction: string;
    size: string;
    children: ReactNode;
    className?: string;
    label?: string;
    error?: ReactNode;
    gap?: Gap;
    disabled?: boolean;
    tooltip?: string;
};
declare const InputWrapper: (props: Props) => JSX.Element;
export { InputWrapper, styles as inputWrapperStyles };
export type { Props as InputWrapperProps };
