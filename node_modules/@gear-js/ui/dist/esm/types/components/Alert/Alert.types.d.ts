import { CSSProperties, ReactNode } from 'react';
declare type Options = {
    type: 'info' | 'error' | 'loading' | 'success';
    style?: CSSProperties;
    title?: string;
    timeout?: number;
    isClosed?: boolean;
};
declare type Alert = {
    id: string;
    content: ReactNode;
    options: Options;
};
declare type Props = {
    alert: Alert;
    close: () => void;
};
export type { Props };
