import { ReactNode } from 'react';
import styles from './Modal.module.scss';
declare type Props = {
    heading: string;
    close: () => void;
    children?: ReactNode;
    className?: string;
    size?: 'normal' | 'large';
};
declare const Modal: ({ heading, close, children, className, size }: Props) => import("react").ReactPortal | null;
export { Modal, styles as modalStyles };
export type { Props as ModalProps };
